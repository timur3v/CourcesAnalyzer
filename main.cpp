#include <Parser.cpp>
#include <CourseAnalyzer.cpp>

struct ToClose {
  size_t level_5;
  std::vector<size_t> level_3;
};

int main() {
  std::unordered_map<size_t, Course> courses;
  std::vector<size_t> preferred_courses;

  std::vector<size_t> to_close_5_level;
  std::vector<size_t> to_close_3_level;

  std::cout << "Hello, World!" << std::endl;
  return 0;
}
